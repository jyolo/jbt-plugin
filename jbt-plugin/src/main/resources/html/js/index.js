
function addDivToBody(from="js") {
    // 创建一个 div 元素
    var div = document.createElement('div');
    // 设置 div 元素的内容
    div.innerHTML = from + '爱仕达多奥术大师多撒奥术大师大所多阿萨德按时按时实打实大所大驱蚊器无其味无穷二群翁太突然又太容易人同意让他一人同意';
    // 将 div 元素添加到 body 元素中
    document.body.appendChild(div);
}